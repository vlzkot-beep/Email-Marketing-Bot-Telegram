# 📋 ИНСТРУКЦИЯ ПО УСТАНОВКЕ

## 🚀 БЫСТРАЯ УСТАНОВКА (5 МИНУТ)

### Шаг 1: Клонируйте репозиторий

```bash
git clone https://github.com/yourusername/email-marketing-bot.git
cd email-marketing-bot
```

### Шаг 2: Создайте виртуальное окружение

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### Шаг 3: Установите зависимости

```bash
pip install -r requirements.txt
```

### Шаг 4: Создайте .env файл

```bash
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

### Шаг 5: Заполните .env

Откройте файл `.env` в текстовом редакторе и заполните:

```env
BOT_TOKEN=7932998723:AAF...
SMTP_EMAIL=your_email@gmail.com
SMTP_PASSWORD=yr..............
```

### Шаг 6: Запустите бота

```bash
python bot.py
```

---

## 📱 ПОСЛЕ ЗАПУСКА

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Выполняйте инструкции бота

---

## 🔧 ПОЛУЧЕНИЕ УЧЕТНЫХ ДАННЫХ

### BOT_TOKEN

1. Откройте Telegram → @BotFather
2. Отправьте `/newbot`
3. Введите имя бота
4. Скопируйте токен

### SMTP_EMAIL и SMTP_PASSWORD

1. Перейдите на https://myaccount.google.com/security
2. Включите 2-Step Verification
3. Перейдите на https://myaccount.google.com/apppasswords
4. Выберите Mail и Windows Computer
5. Скопируйте пароль

---

## ✅ ПРОВЕРКА УСТАНОВКИ

Если видите это сообщение:
```
✅ Бот готов к работе. Слушаю сообщения...
```

Значит всё установлено правильно! 🎉

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Ошибка: "No module named 'telegram'"

```bash
pip install python-telegram-bot
```

### Ошибка: "No module named 'pandas'"

```bash
pip install pandas openpyxl
```

### Ошибка: "отсутствуют переменные окружения"

1. Убедитесь, что создан файл `.env`
2. Заполните все значения
3. Перезагрузите бота

### Ошибка: "SMTP: Authentication failed"

1. Проверьте SMTP_EMAIL и SMTP_PASSWORD
2. Убедитесь, что используется пароль приложения (не основной пароль)
3. Пересоздайте пароль приложения

---

## 📊 СТРУКТУРА ПРОЕКТА

```
email-marketing-bot/
├── bot.py                    ✅ Основной код
├── config.py                 ✅ Конфигурация
├── requirements.txt          ✅ Зависимости
├── .env.example             ✅ Шаблон конфигурации
├── .env                      ❌ Локальный файл (не коммитить)
├── .gitignore               ✅ Игнор для Git
├── README.md                ✅ Документация
├── LICENSE                  ✅ Лицензия
├── docs/
│   ├── BOT_WORKFLOW.md      ✅ Как работает
│   ├── SCREENSHOTS.md       ✅ Скриншоты
│   └── INSTALLATION.md      ✅ Этот файл
├── email_bot.log            ❌ Логи
└── user_data/               ❌ Временные файлы
```

---

## 🔐 SECURITY CHECKLIST

Перед использованием проверьте:

- ✅ .env файл создан и заполнен
- ✅ .env добавлен в .gitignore
- ✅ Никаких реальных данных в config.py
- ✅ requirements.txt установлен
- ✅ Бот запускается без ошибок

---

## 📞 БЫСТРАЯ ПОМОЩЬ

| Команда | Результат |
|---------|-----------|
| `python bot.py` | Запуск бота |
| `pip install -r requirements.txt` | Установка зависимостей |
| `python -m venv venv` | Создание виртуального окружения |
| `/send` в Telegram | Начало рассылки |
| `/cancel` в Telegram | Отмена операции |

---

**Версия:** 1.0  
**Дата:** 2026-01-06  
**Статус:** ✅ Ready
